"""
    add_vertex!(g, v)

Add the vertex `v` to the graph `g`.
"""
function add_vertex! end

"""
    rm_vertex!(g, v)

Remove the vertex `v` from the graph `g`.
"""
function rm_vertex! end

"""
    add_edge!(g, e)

Add the edge `e` to the graph `g`.
"""
function add_edge! end

"""
    rm_edge!(g, e)

Remove the edge `e` from the graph `g`.
"""
function rm_edge! end
